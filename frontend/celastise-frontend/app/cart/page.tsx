'use client';
import Link from 'next/link';
import { useCart } from '@/store/cart';
export default function CartPage(){ const {items, inc, dec, remove, total}=useCart(); return <main className="min-h-screen bg-celastise-50 py-12"><div className="max-w-5xl mx-auto px-4"><h1 className="text-4xl font-bold mb-8">Shopping Cart</h1>{items.length===0?<p>Your cart is empty. <Link className="text-rose-500" href="/collections">Browse watches</Link></p>:<><div className="space-y-4">{items.map(i=><div key={i.product_id} className="bg-white rounded-xl p-4 flex gap-4 items-center shadow"><img src={i.image} className="w-28 h-28 object-cover rounded-lg"/><div className="flex-1"><h3 className="font-bold text-xl">{i.name}</h3><p className="text-celastise-600">{i.brand_name}</p><p className="font-semibold">${i.price.toLocaleString()}</p></div><div className="flex items-center gap-2"><button onClick={()=>dec(i.product_id)} className="px-3 py-2 border rounded">-</button><span className="w-8 text-center">{i.quantity}</span><button onClick={()=>inc(i.product_id)} className="px-3 py-2 border rounded">+</button></div><button onClick={()=>remove(i.product_id)} className="text-rose-500">Remove</button></div>)}</div><div className="mt-8 bg-white p-6 rounded-xl shadow flex items-center justify-between"><p className="text-2xl font-bold">Total: ${total().toLocaleString()}</p><Link href="/checkout" className="px-8 py-4 bg-rose-500 text-white rounded-lg font-bold">Proceed to Checkout Payment</Link></div></>}</div></main>}
