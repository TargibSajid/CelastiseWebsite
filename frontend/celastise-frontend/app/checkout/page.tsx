'use client';
import { FormEvent, useState } from 'react';
import { useCart } from '@/store/cart';
import { api } from '@/lib/api';
export default function CheckoutPage(){ const {items,total,clear}=useCart(); const [msg,setMsg]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); const payload={ customer_name:fd.get('name'), phone:fd.get('phone'), address:fd.get('address'), house_no:fd.get('house_no'), payment_method:fd.get('payment_method'), items, total:total()}; try{const r:any=await api.createOrder(payload); clear(); setMsg(`Order placed successfully. Order ID: ${r.order_id}`)}catch{setMsg('Backend not running or order table missing. Start NestJS backend and import SQL.')}} return <main className="min-h-screen bg-celastise-50 py-12"><form onSubmit={submit} className="max-w-3xl mx-auto bg-white p-8 rounded-2xl shadow space-y-5"><h1 className="text-4xl font-bold">Checkout</h1><input required name="name" placeholder="Full name" className="w-full border p-3 rounded"/><input required name="phone" placeholder="Phone number" className="w-full border p-3 rounded"/><input name="house_no" placeholder="House / Flat number" className="w-full border p-3 rounded"/><textarea required name="address" placeholder="Home address" className="w-full border p-3 rounded h-28"/><select name="payment_method" className="w-full border p-3 rounded"><option value="cod">Cash on Delivery</option><option value="pay_now">Pay Now / Instant Payment</option></select><div className="text-xl font-bold">Total: ${total().toLocaleString()}</div><button className="w-full py-4 bg-rose-500 text-white rounded-lg font-bold">Place Order</button>{msg&&<p className="text-rose-600 font-semibold">{msg}</p>}</form></main>}
