'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { api } from '@/lib/api';
export default function SignupPage(){ const [msg,setMsg]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); try{await api.signup(String(fd.get('name')),String(fd.get('email')),String(fd.get('password')),String(fd.get('phone')||''),String(fd.get('address')||'')); setMsg('Account created. Session cookie saved.')}catch{setMsg('Signup failed. Email may already exist or backend is not running.')}} return <main className="min-h-screen bg-celastise-50 py-16"><form onSubmit={submit} className="max-w-md mx-auto bg-white p-8 rounded-2xl shadow space-y-5"><h1 className="text-3xl font-bold">Sign Up</h1><input required name="name" placeholder="Full name" className="w-full border p-3 rounded"/><input required name="email" type="email" placeholder="Email" className="w-full border p-3 rounded"/><input required name="password" type="password" placeholder="Password" className="w-full border p-3 rounded"/><input name="phone" placeholder="Phone" className="w-full border p-3 rounded"/><textarea name="address" placeholder="Saved delivery address" className="w-full border p-3 rounded"/><button className="w-full py-3 bg-rose-500 text-white rounded-lg font-bold">Create Account</button><p>Already registered? <Link href="/login" className="text-rose-500">Login</Link></p>{msg&&<p className="text-sm text-celastise-700">{msg}</p>}</form></main>}
